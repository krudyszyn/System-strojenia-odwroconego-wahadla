global A B

A = [0, g * (M + m) / (M * L), b / (M * L), 0;
    1, 0, 0, 0;
    0, -g * m / M, -b / M, 0;
    0, 0, 1, 0];

B = [-1/(M * L);    0;   1/M;     0];

C = [0 1 0 0; 0 0 0 1];
D = [0;0];

